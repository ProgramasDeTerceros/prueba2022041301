import { ImgNofoundPipe } from './img-nofound.pipe';

describe('ImgNofoundPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgNofoundPipe();
    expect(pipe).toBeTruthy();
  });
});
